{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "validate with params",
            "type": "shell",
            "command": "terraform validate -var GITHUB_ORGANIZATION=$env:TF_VAR_GITHUB_ORGANIZATION -var GITHUB_TOKEN=$env:GITHUB_TOKEN"
        }
    ]
}